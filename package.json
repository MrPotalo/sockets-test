{
    "name": "socket-test",
    "version": "1.0.0",
    "main": "index.js",
    "license": "MIT",
    "scripts": {
      "start": "concurrently \"cd server && yarn start\" \"cd client && yarn start\"",
      "all": "yarn && cd client && yarn && cd ../server && yarn"
    },
    "dependencies": {
      "concurrently": "^4.0.1"
    }
  }
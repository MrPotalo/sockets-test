{
    "name": "socket-test",
    "version": "1.0.0",
    "main": "index.js",
    "license": "MIT",
    "scripts": {
      "start": "concurrently \"cd server && yarn start\" \"cd client && yarn start\"",
      "all": "cd client && yarn && cd ../server && yarn && cd ..",
      "postinstall": "yarn all && cd client && yarn build && cd .."
    },
    "engines": {
      "node": "~8.12.0"
    },
    "dependencies": {
      "concurrently": "^4.0.1"
    }
  }